springdoc:
  swagger-ui:
    custom-css-url: /swagger-ui/swagger-overrides.css

spring:
  main:
    allow-circular-references: true

  data:
    mongodb:
      uri: mongodb+srv://root:dip1234@cluster0.tdqouji.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0
      database: JobPortal

# REMOVE SMTP BLOCK — Render blocks SMTP ports
#
# spring:
#   mail:
#     host: smtp.sendgrid.net
#     port: 587
#     username: apikey
#     password: ${SENDGRID_API_KEY}
#     properties:
#       mail.smtp.auth: true
#       mail.smtp.starttls.enable: true

# ✔ Use SendGrid API (works on Render)
sendgrid:
  api-key: ${SENDGRID_API_KEY}
  sender: ${SENDGRID_SENDER:dipkotadiya187@gmail.com}

server:
  port: ${PORT:8080}
  servlet:
    context-path: /jobPortal
